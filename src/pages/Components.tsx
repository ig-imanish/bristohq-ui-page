export default function Components() {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Components</h1>
            <p>Browse all available UI components.</p>
        </div>
    );
}
